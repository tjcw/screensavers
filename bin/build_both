#!/bin/bash -x

function build_one
{
  (
   mkdir -p "${stamp}" &&
   cd "${stamp}" &&
   /home/${SUDO_USER}/eclipse-workspace/screensavers/bin/do_oi "$1" bookworm 
  )
}

stamp="$(date +%Y%m%d-%H%M)"
build_one freeduc_bookworm
build_one gk_bookworm

